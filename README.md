# PR Charts Repository
This branch contains packaged Helm charts from PRs for testing purposes.

Charts are versioned with -prXXX suffix and are automatically cleaned up when PRs are merged or closed.
