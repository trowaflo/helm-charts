suite: test common chart - all resources
templates:
  - templates/common.yaml
values:
  - values/default.yaml
release:
  name: common-test
  namespace: common-test
tests:
  # Snapshot = validates complete coherence with default values
  - it: should match snapshot with default values
    asserts:
      - matchSnapshot: {}
