suite: test mdns-repeater chart - all resources
templates:
  - templates/common.yaml
values:
  - values/default.yaml
release:
  name: mdns-repeater-testing
  namespace: mdns-repeater-testing
tests:
  - it: should match snapshot with default values
    asserts:
      - matchSnapshot: {}
