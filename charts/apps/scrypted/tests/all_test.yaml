suite: test scrypted chart - all resources
templates:
  - templates/common.yaml
values:
  - values/default.yaml
release:
  name: scrypted-testing
  namespace: scrypted-testing
tests:
  - it: should match snapshot with default values
    asserts:
      - matchSnapshot: {}
