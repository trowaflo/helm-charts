suite: test frigate-exporter chart - ServiceMonitor
templates:
  - templates/common.yaml
tests:
  # Snapshot = validates complete coherence with default values
  - it: should match snapshot when enabled
    documentIndex: 2
    asserts:
      - matchSnapshot: {}
