---
# Test values with all features enabled
cronjobs:
  backup:
    enabled: true
    podSpec:
      volumes:
        - name: backup
          nfs:
            server: "192.168.1.100"
            path: "/mnt/backups"
