---
jobs:
  restore:
    enabled: true
    podSpec:
      volumes:
        - name: backup
          nfs:
            server: "192.168.1.100"
            path: "/mnt/backups"

restore:
  restoreFile: ghostfolio-2026-04-21.dump
