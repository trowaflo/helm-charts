---
# Test values with minimal configuration
ghostfolio:
  secretName: test-ghostfolio-secrets
  database:
    secretName: test-db-secret
    secretKey: DATABASE_URL
  redis:
    passwordSecretName: test-redis-secret
    passwordSecretKey: REDIS_PASSWORD

redis:
  enabled: false

postgresql:
  enabled: false

backup:
  enabled: false

restore:
  enabled: false
