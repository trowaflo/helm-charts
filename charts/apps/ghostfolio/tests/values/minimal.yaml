---
# Test values with minimal configuration
statefulsets:
  redis:
    enabled: false

cnpgClusters:
  postgresql:
    enabled: false

cronjobs:
  backup:
    enabled: false

jobs:
  restore:
    enabled: false
