---
# Test values with minimal configuration
# Disable valkey subchart
redis:
  enabled: false

cnpgClusters:
  postgresql:
    enabled: false

deployments:
  main:
    enabled: false

cronjobs:
  backup:
    enabled: false

jobs:
  restore:
    enabled: false
