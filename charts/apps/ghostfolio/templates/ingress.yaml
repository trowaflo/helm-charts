{{- if .Values.ghostfolio.ingress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "ghostfolio.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "ghostfolio.labels" . | nindent 4 }}
  {{- with .Values.ghostfolio.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.ghostfolio.ingress.className }}
  ingressClassName: {{ .Values.ghostfolio.ingress.className }}
  {{- end }}
  {{- if .Values.ghostfolio.ingress.tls }}
  tls:
    {{- range .Values.ghostfolio.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.ghostfolio.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: {{ include "ghostfolio.fullname" $ }}
                port:
                  number: {{ $.Values.ghostfolio.service.port }}
          {{- end }}
    {{- end }}
{{- end }}
