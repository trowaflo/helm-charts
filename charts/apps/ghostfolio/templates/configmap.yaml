apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "ghostfolio.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "ghostfolio.labels" . | nindent 4 }}
data:
  # Placeholder ConfigMap for future configuration needs
  # Currently used for checksum annotation in deployment
  app.conf: |
    # Ghostfolio configuration
