apiVersion: v1
kind: Service
metadata:
  name: {{ include "ghostfolio.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "ghostfolio.labels" . | nindent 4 }}
  {{- with .Values.ghostfolio.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.ghostfolio.service.type }}
  ports:
    - port: {{ .Values.ghostfolio.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ghostfolio.selectorLabels" . | nindent 4 }}
