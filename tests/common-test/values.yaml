---
# Test values for common-test chart
# This chart validates common library functionality
# All resources are disabled by default for testing
k8sServices:
  main:
    enabled: false

ingresses:
  main:
    enabled: false

serviceMonitors:
  main:
    enabled: false
